# Product and technical gap baseline

Snapshot date: 2026-09-10. Re-read live refs, PRs, reviews/threads, exact-head checks, rulesets, security evidence and releases before merge, release, restack or foreign-owner handoff. This is Wardnet's sole commercial/product-technical current-state ledger; detailed RED→GREEN receipts stay in the owning PR and issue histories. Exact Draft heads below are provisional evidence, not protected or released truth, and predecessor GREEN never transfers after head/base movement.

## Authority boundary

Wardnet owns the Rust-first gateway/SOC control plane, Agent Artifact Admission, outbound destination-reputation assessment, Wardnet security-evidence lifecycle, policy decisions and SOC accountability. `quarantine-sandbox-runtime` owns hostile execution/isolation/cleanup; EgressWeave owns executable outbound URL/address/DNS/peer/redirect/proxy/TLS/resource authorization; `contextual-orchestrator` owns Agent/LLM/provider orchestration; `appguardrail` owns static package/security analysis. Wardnet consumes released/versioned ports or ACLs only and does not copy sibling source, query foreign application tables, or pin mutable sibling heads as production dependencies.

Protected ADR truth keeps browser acquisition, sessions, anti-bot challenges and CAPTCHA handling outside Wardnet. A Wardnet reputation `allow` is not transport authorization: protect-mode forwarding must compose Wardnet policy with independently authoritative EgressWeave authorization, and neither authority can override the other's deny.

`context-graph-contracts` is the provider-neutral Context Fabric Shared Kernel and `enterprise-architecture-core` is the EA Decision Plane. Wardnet reads those owner paths for compatibility and handoff only. Wardnet findings, IOCs, destination assessments and incidents remain Wardnet truth; architecture-relevant lifecycle/ownership/risk/remediation may be projected only through released compatible contracts/provenance.

## Protected truth, governance and release state

Protected/default Wardnet truth remains `main@a52ccd0a24a727d9349bb32def7713882d8cad1e`. Wardnet has no GitHub Release, so protected source truth is not yet an immutable release identity.

Organization ruleset `18156473` remains active on the default branch and requires one generic approving review while declaring no required reviewer/team/code-owner/last-push reviewer. It retains required review-thread resolution, centrally required OpenCode/Noema/Strix/security/SAST/CodeQL workflows, deletion protection and non-fast-forward protection, and exposes `OrganizationAdmin/always` bypass. `.github#772` remains the canonical solo-maintainer governance defect; `.github#1644@e35cdc5d6527dfa8634654719a6c3681f16ed82a` remains Draft and has not yet changed the live rule. Self-approval, model/bot-as-human approval and routine administrator bypass are forbidden. A guarded emergency bypass is not justified by an ordinary approval deadlock.

Gap-ledger PR #130 is the sole writer for this file. Its predecessor Ready head `d44a36fc6b19f56ce5ce358c24642019b265d96c` had terminal same-head CI/Rust, CodeQL, SAST, Security Scan, coverage evidence/source-tree, Required OpenCode, Required Noema and Required Strix workflow evidence with no unresolved inline review thread. This refresh advances that head, so all `d44a36...` results are historical and the successor exact head must reacquire every then-applicable gate before an ordinary protected merge.

Runner/materialization remains `.github#712` authority; delegated CodeQL terminal-verdict ordering/publication remains central owner authority under `.github#1929` and successors. Model-side route/provider failures observed by Wardnet required reviews remain `contextual-orchestrator#1106` authority. Wardnet does not add leaf provider/model/group fallbacks, duplicate central workflows, synthesize statuses, or source-churn a clean candidate to repair those planes.

## Context Fabric and EA compatibility

`context-graph-contracts` remains protected/default `develop@99cb5468ba3c15c5e79688f53dee74724fae2d13` with no GitHub Release. Its Context Assertion/conformance/source-provenance stack is Draft/unreleased; current candidate #21 is `238773711aec5c22482fa073ccf3e73a0878d812`. The intended protected/default branch mismatch is foreign-owner governance work under `.github#1137`. Wardnet must consume only immutable released CGC contracts; mutable CGC heads are compatibility evidence only.

`enterprise-architecture-core` remains protected/default `develop@dd71e40a86385fb7861b0f1be19891a3f3e29ece` with no GitHub Release. EA issue #49 already owns the architecture-only Wardnet outbound site-reputation projection boundary: Wardnet verdict/risk truth stays Wardnet; EgressWeave enforcement truth stays EgressWeave; EA receives lifecycle/ownership/remediation architecture only after immutable Wardnet and CGC identities exist. Wardnet does not write EA source/PR state or promote mutable findings into authoritative EA truth.

## Outbound destination reputation

#173 remains Proposed architecture for the Wardnet-owned reputation engine and does not claim runtime enforcement. #175 is the root pure-Rust `wardnet.reputation.v1` contract. The dependent evidence stack remains ordered: #176 business-authorization binding → #178 decision freshness → #179 evidence-snapshot health → #183 exact source-generation membership → #185 lifecycle-invalid rejection → #187 producer-version/tombstone lifecycle cursor → #189 atomic complete-source replacement → #191 source-generation ABA replay. #191 proves that bounded current-only lifecycle memory cannot enforce durable historical opaque generation uniqueness; durable production authority therefore remains the PostgreSQL #80/#192 line.

Production composition still requires immutable compatible EgressWeave authorization/evidence. Wardnet never turns its reputation verdict into transport authority.

## PostgreSQL production-state prerequisite stack

Current canonical branch order is `#140 -> #193 -> #194 -> #196 -> #198 -> #199 -> #200 -> #207 -> #208 -> #209 -> #212 -> #216 -> #217 -> #219 -> #221 -> #223 -> #224 -> #225 -> #226 -> #228 -> #229 -> #231 -> #233 -> #234 -> #236 -> #241 -> #242 -> #244`. #227/#230/#232/#235/#239/#240/#243 are acceptance/RED lanes whose valid deltas are carried by the named successors rather than additional ancestry nodes. Every parent movement requires ordinary non-force adoption and fresh exact-head evidence.

The stack establishes explicit fail-closed PostgreSQL authority selection, credential indirection/validation, immutable generation identity, atomic publication/head persistence under FORCE RLS, least-privilege roles, migration rollback/reapply and startup serialization, externally managed runtime LOGIN mapping, transaction-local tenant binding, typed publication/replay/conflict semantics, mandatory actor/decision attribution, removal of arbitrary raw-SQL escape hatches, authoritative-complete reads, fixed-pool fail-closed behavior, bounded per-slot replenishment and bounded readiness without replaying started database work.

Current late-stack evidence is:

- #236 Draft exact `e1131c2ce1078322b48caa87b2088b1866b08626`, child of #234. Exact CI `34336708591` and Fuzz `34336708581` are terminal SUCCESS. It owns typed `CommitOutcomeUnknown` for transport loss while awaiting COMMIT and explicitly forbids automatic replay.
- #241 Draft exact `992d9c366c47515a121b786197c872db81957dcc`, child of #236. Exact CI `34375192687` is terminal SUCCESS on the destructive PostgreSQL 18.4 physical base-backup/WAL/PITR recovery drill and unsafe-role mapping guard. This is controlled recovery-fixture evidence, not production RPO/RTO, storage-provider, encryption-key or IAM authority.
- #242 Draft exact `641d7cd4b12d174015801ce4aee8f29803f239ad`, child of #241. Exact CI `34381212642` is terminal SUCCESS on real PostgreSQL 18.4 divergent-writer serialization and byte-identical duplicate behavior without introducing a global writer lock.
- #244 Draft exact `91cab184c95984d5ffdb6b2a4eb8f8920fbae8f5`, child of #242. Production uses bounded non-mutating `Client::check_connection()` before Wardnet state work, removes a timed-out/desynchronised client before slot reuse, preserves one replacement authority per slot, shares the fixed readiness window across candidate sessions, and never replays started database work. The real protocol-blackhole fixture proves selective failover, all-stream fail-closed behavior, bounded recovery without restart/reconnect storm, and a slow-valid positive control. It also measures 200 unexcluded real PostgreSQL preflight+probe samples at p95 `<= 20 ms` while retaining exactly the original two physical runtime sessions. Current exact CI `34402494574` and Fuzz `34402494583` are terminal SUCCESS.

#244 also introduces canonical `docs/TEST_STRATEGY.md`. It makes the commercial test contract explicit: `cargo test` is not implicit proof of 100% coverage; issue #243 remains open until one exact head has explicit 100% owned production statement/branch/edge/rustdoc evidence. The document binds real PostgreSQL 18.4 hostile schedules, no-replay COMMIT ambiguity, the 200-sample buyer-path performance denominator, recovery/operability boundaries, and NIST/PostgreSQL/tokio-postgres/IETF traceability.

`StateAuthority::Postgres` remains disabled for production. Draft branch GREEN is not protected truth. Remaining production-state gaps are dependency-first protected integration of the full lineage; explicit exact-head 100% owned production statement/branch/edge/rustdoc evidence; production backup/WAL retention and storage authority; encryption-key and IAM authority; production RPO/RTO/SLO and restore evidence; and one immutable release identity tying source, artifact, security, coverage, package, SBOM, provenance, reproducibility and rollback together.

## Admin console buyer path

Draft #127 remains exact `ff47a9b6cc4cf978c0d3cf81c0556cc57c6e59cf`. Its browser harness security repair removed the explicit empty ChromeDriver `--allowed-ips=` switch and keeps the WebDriver endpoint loopback-only. On that exact head, CI `34399428655` passes loading, normal, permission-denied, keyboard/skip-link focus transfer, computed accessible name/description and atomic polite KPI semantics, then fails only the 375 px horizontal-overflow assertion. Fuzz `34399428636`, Security Scan `34399428709` and SAST Semgrep `34399428685` are terminal SUCCESS.

The remaining UI defect is causal production CSS: `header.app` is a non-wrapping flex row while the heading, gap, toolbar and 200 px admin-token input cannot fit the 327 px inner width at a 375 px viewport. The minimum source repair is to allow the header row to wrap and rerun the exact-current 375/768/1440 real-browser acceptance. Hiding overflow, zooming, widening the test viewport or injecting test-only CSS is not acceptable. CodeQL delegated terminal-verdict evidence and its two prior CWE-319 review threads also remain non-passing/unresolved on #127. UI Delivery Gate: **FAIL** until both the source overflow and exact-current security-review state are GREEN.

## Process lifecycle and graceful shutdown

Draft #245 is a clean test-first child of protected `main@a52ccd0a24a727d9349bb32def7713882d8cad1e`. Test-only exact head `d9a943ed92950fd557ac9ad2d19f073f647b5054` adds a real shipped-binary Unix SIGINT acceptance beside the existing SIGTERM acceptance: the fixture waits for Wardnet's actual readiness line, delivers SIGINT, and requires a clean process exit. Production remains byte-identical to protected main on this head.

Protected `src/main.rs` installs a Tokio SIGTERM handler synchronously before readiness on Unix but leaves SIGINT at the OS default disposition. The expected semantic RED is therefore that SIGINT terminates the process outside Wardnet's Axum graceful-shutdown future. The current repository CI for the test-only head is queued and has not yet produced executable semantic RED evidence; runner/bootstrap/formatting failure would not count as that RED. Do not advance the production source merely to bypass the queued RED. Once the exact test-only run executes and confirms the signal-path failure, the minimum causal repair is to register both SIGTERM and SIGINT before readiness and resolve the one existing shutdown future when either arrives, leaving Windows behavior unchanged. Exact-head formatting, locked workspace tests/Clippy and all then-applicable security/review/thread/protected-base evidence must be reacquired before promotion.

## Other P0 security/release lanes

Model-backed authentication/session-integrity and attack-path stacks remain Draft/unreleased and therefore cannot be promoted to protected product truth from branch evidence alone. Their detailed exact receipts stay in #87 and their owning PR/issue histories. Any integration must preserve Wardnet's bounded-context ownership, fail closed on mutable/unreleased foreign contracts, retain purpose-bound PII/security evidence semantics, and reacquire current-head hostile, security, coverage and thread evidence after each parent/base movement.

All Wardnet LLM-backed workflows remain consumers of released `contextual-orchestrator` API/client/schema and `orchestrator/free` plus gateway token. Wardnet does not select providers/models/groups or paid fallbacks locally. Capability absence or provider-routing defects fail closed and are repaired at the CO owner path.

## Release gate and next order

Wardnet is **not release-ready**. There is no immutable Wardnet GitHub Release. A release-ready protected head must have one immutable source/artifact identity with then-live deterministic CI, hostile/security/SAST/CodeQL, explicit owned-production coverage/rustdoc, package, SBOM, provenance, reproducibility, rollback, operability/recovery and unresolved-thread evidence. Mutable CGC/EA/EgressWeave/CO heads cannot satisfy a released-contract dependency.

Execution order stays repair-first rather than report-first: advance `.github#772` through its central owner automation while continuing independent Wardnet work; keep #130 as the single current-state ledger and merge it only through the ordinary protected path once its successor exact head and live governance permit; complete #245's executable SIGINT RED and minimum graceful-shutdown repair; continue #244/#243 with explicit coverage/rustdoc plus the remaining production storage/recovery/release invariants; repair #127's real 375 px header overflow when a safe exact-source mutation path is available; then integrate dependency-first and create/verify the immutable release without force updates, destructive rebases, gate weakening, self/model approval, routine bypass, source copying, cross-service SQL or predecessor-evidence transfer.
