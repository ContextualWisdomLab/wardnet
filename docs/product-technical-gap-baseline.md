# Product and technical gap baseline

Snapshot date: 2026-09-06. Re-read live refs, PRs, reviews/threads, exact-head checks, rulesets, security evidence and releases before any merge, release, restack or foreign-owner handoff. This is Wardnet's sole commercial/product-technical current-state ledger, not an archive of predecessor evidence.

## Authority boundary

Wardnet owns the Rust-first gateway/SOC control plane and the Gateway, Admission Policy, Artifact Identity, Security Analysis Integration, Network-Egress integration, SOC Evidence, Runtime Control, Audit-Provenance, Agent Artifact Admission and destination-reputation policy/evidence boundaries. `quarantine-sandbox-runtime` owns hostile execution isolation and cleanup; `contextual-orchestrator` owns Agent/LLM/provider orchestration; EgressWeave owns reusable outbound URL/address/DNS/peer/redirect/proxy/TLS/resource authorization; `appguardrail` owns static package/security analysis. Wardnet consumes released/versioned ports or ACLs only: no source copy, cross-service SQL or mutable sibling production dependency.

Browser acquisition, sessions, anti-bot challenges and CAPTCHA handling remain outside Wardnet by protected ADR #171. Wardnet may classify destination maliciousness and make reputation-policy decisions, but those decisions are not proof that transport was blocked. A production enforcement point must compose Wardnet policy with the independently authoritative EgressWeave transport decision; neither side may override the other's deny.

`context-graph-contracts` is the provider-neutral Context Fabric Shared Kernel and `enterprise-architecture-core` is the EA Decision Plane. Both remain read-only from this Wardnet writer while their Context Fabric owner is active. Wardnet findings/verdicts remain Wardnet evidence; EA may retain verified evidence references for technology risk/remediation/initiative context but must not copy raw security verdicts into authoritative architecture truth.

Fresh GitHub release inventories are empty for Wardnet, Context Graph Contracts, EA Core and EgressWeave. No open foreign-owner PR head is production/release authority.

## Protected truth and control plane

Protected/default Wardnet truth remains #171 squash `main@a52ccd0a24a727d9349bb32def7713882d8cad1e`. Its protected decision keeps anti-bot acquisition outside Wardnet while destination maliciousness/reputation policy and security evidence remain Wardnet-owned.

Organization ruleset `18156473` remains active on `~DEFAULT_BRANCH`. The fresh payload still requires `required_approving_review_count=1` while `required_reviewers=[]`, with no code-owner or last-push approver requirement; it also requires thread resolution and seven central workflows and prohibits deletion/non-fast-forward updates. At the same time it exposes `OrganizationAdmin/always` bypass to the current principal. Under the declared solo-maintainer model, self-approval and bot/model-as-human approval remain forbidden.

#171 remains the concrete governance-defect specimen: an expected-head squash merge reached protected truth while the same ruleset still exposed the structurally impossible generic approval count and routine admin bypass. `.github#772` remains the causal owner path. Wardnet will not issue merge calls merely to test whether the API silently traverses that bypass. The accepted repair is limited to solo-compatible approval/bypass semantics while deterministic workflow/security/coverage/package/SBOM/provenance/thread/deletion/non-fast-forward controls remain fail closed.

Runner/materialization starvation remains `.github#712` owner work. Current Wardnet #175 exact `cba547dcaadb8a6868de44604c28d7dcd4d14244` supplies the current specimen after a substantive tenant-scope security repair: CI job `101484684987` (`ubuntu-24.04`) and focused causal verifier job `101484682906` (`macos-15-arm64`) are both pre-checkout `queued` with no assigned runner/group and `steps=[]`. The exact owner handoff is `.github#712` comment `5559148587`; Wardnet will not source-churn or rerun-storm to manufacture execution.

Protected-main push validation on `main@a52ccd0...` has previously reached terminal GREEN for CI, Scorecard and dynamic CodeQL. That is protected-head execution evidence only: normal-vs-bypass governance remains unresolved, Wardnet has no immutable release, and mandatory product/security gaps remain candidate-only.

## Agent Artifact Admission

PR #129 is now exact `14c0af32e7e4f68c682d55a1b1117629fa940336`, Draft, non-force adopted onto current protected `main@a52ccd0...`. All predecessor workflow conclusions became historical after that ancestry movement. Its fresh current-head CI/Fuzz/Security/Semgrep/CodeQL runs are queued; CI job `101450870709` is pre-checkout with `runner_id=0` and `steps=[]`, already delegated to `.github#712`.

The bounded context is pre-execution structured installer security admission only. It binds reviewed workspace manifest/artifact identity, exact ecosystem/name/version/registry/owner/SHA-256, executable family, declared operands, policy revision and bounded provenance; it does not fetch, decrypt, install, execute, isolate, activate or route workloads. An `allow` receipt is admission authority, not downloaded-byte proof or runtime activation. AppGuardrail, quarantine, Noema and Context Fabric retain their own authorities.

## Immediate product/security lanes

- **#175 outbound reputation contract core** — current exact Draft/mergeable head `cba547dcaadb8a6868de44604c28d7dcd4d14244` on exact protected base `a52ccd0...`. Fresh comparison to Proposed #173 exposed a real scope gap: the initial `SourcePolicyV1` expressed allowed purposes and subject kinds but could not express #173's required allowed-tenant boundary. Test-first `c9a02378705623b934e2d4f05743c74c36e5026d` records the omission as a failing contract expectation; the final exact candidate now requires explicit `tenant_scope` plus `allowed_tenant_ids`, rejects an omitted scope, rejects empty explicit sets and contradictory all-tenant-plus-list states, updates the round-trip fixture, and records the CWE-20/SSDF fail-closed rationale. The RED commit's ordinary runs were cancelled by subsequent substantive source movement before execution, so no executed RED is claimed. Current exact focused verifier `34032553791`, CI `34032553766`, Fuzz `34032553600`, Security `34032553668`, Semgrep `34032553602` and CodeQL `34032553694` are queued; prior check conclusions do not transfer. The core still does not parse/authorize URLs, resolve DNS, connect, redirect, proxy, establish TLS, perform HTTP I/O or claim actual blocking. All current inline review threads are resolved. Remove the temporary error-precedence verifier only after its exact current-head run reaches terminal GREEN, then reacquire the complete gate set on the cleanup head.
- **#173 outbound site-reputation architecture** — exact `7d0006b0f1fd3311c891bf359bd0c3e66a1831ec`, Ready/documentation-only on current protected base. Applicable CI/Security/Semgrep/CodeQL lanes are terminal GREEN, but the ADR remains Proposed and EgressWeave has no immutable release. #175 implements only the pure Wardnet-owned contract slice; it does not make #173 Accepted or production-enforced.
- **#167 MISP admission + shared DNSBL snapshot ownership** — exact current Draft `0c83cd5956f512d79c6600e823fcfa6d6f32af4e` on protected `main@a52ccd0...`. CI/Fuzz/Security/Semgrep are terminal GREEN; CodeQL fails only at the authenticated delegated current-head verdict boundary. The source repair preserves affirmative MISP `to_ids`, active lifecycle, feed/operator ownership, stale withdrawal, restart and rollback/retry semantics. Keep Draft until delegated verdict and governance are terminal-valid.
- **#170 MISP threat-level severity child** — exact `2c8d499a5772b0be64d7cc3fc42ed2825ba1499e`, Draft on exact parent #167. Child CI/Fuzz are terminal GREEN; parent must reach protected truth first, then this child must non-force retarget/restack and reacquire its own then-live gates.
- **#174 CodeQL SARIF uploader successor** — exact `028caa05167f9e8f2589b681a8f79c633f406c30`, Draft, one commit ahead/zero behind protected main. It changes only the immutable `github/codeql-action/upload-sarif` pin to v4.37.9. CI/Security/Semgrep are terminal GREEN; CodeQL fails closed only because the exact authenticated delegated verdict remains pending. `.github#1929` owns that central wake/verdict path. #141 remains predecessor evidence until complete protected transfer.
- **#129 Agent Artifact Admission** — current exact `14c0af32e7e4f68c682d55a1b1117629fa940336`, Draft, current-base adopted. Current exact workflows are queued and predecessor GREEN does not transfer.
- **#93 deterministic persistence failure seam** — current exact `dae1256f65e4eab632a453ca1fd13852c1bf76ed`, non-force adopted onto current protected main. The feature replaces permission-dependent test failures with deterministic temp-write/rename fault injection; fresh exact-head workflows are queued and `.github#712` owns the pre-checkout runner specimen.
- **#140 Runtime Configuration** — current exact `a904558b79ef392515dff4b501692112ad6beabf`, current-base adopted. It remains the canonical supporting bootstrap snapshot boundary and prerequisite for trusted-proxy work. Fresh exact-head execution is not yet passing; dependent lanes must not create a competing Runtime Configuration owner.
- **#155 management authentication** — current exact `fb93b61a4a4da30a3471453051ebfb0ed3f63d34`, current-base adopted. Non-loopback management writes remain fail-closed without a write-capable administrator credential. All predecessor GREEN became historical after adoption; current exact evidence must materialize and execute.
- **#166 Wardnet-local draft queue policy** — current exact `e892be0875c14967a208fe89570740473839aa0e`, current-base adopted. It bounds only Wardnet-owned CI/Fuzz queue pressure; central required-workflow runner/materialization remains `.github#712` authority. Current exact lanes are queued.
- **#77 Rust toolchain/reproducibility foundation** — current exact `2856babd52b3131845a347c6d005b031b7099986`, current-base adopted. Previous toolchain/security evidence is predecessor-only after the ancestry move; current exact gate set is non-passing. Release-evidence child #164 stays Draft until this or a verified successor reaches protected truth.
- **#165/#157 trusted client attribution** — #165 remains the production feature owner while #157 preserves unique trusted-proxy fuzz/corpus/invariant evidence. #165 must adopt protected #140 when available and prove complete transfer of every valid #157 item before the preservation lane can retire.
- **#135 bounded local limiter** — remains separate from trusted-proxy attribution and future distributed/global quota authority; do not turn local cardinality state into Noema/distributed admission authority.
- **#136 outbound policy preservation** — preserve Wardnet-owned purpose/call-site/deadline/evidence hostile tests, but do not integrate its local reusable URL/DNS/redirect/proxy/TLS implementation. EgressWeave #237 remains the canonical immutable Rust-consumer authorization/evidence owner path.
- **#115 official threat-source refresh preservation** — preserve Wardnet-owned registry/parser/conditional-validator/last-known-good/SOC evidence, then reconstruct outbound authorization through a released EgressWeave port/ACL rather than the stale local transport implementation.
- **#88 contextual-orchestrator consumer preservation** — preserve unique credential/header/streaming negative evidence, but remove LiteLLM/provider-routing/virtual-key ownership when reconstructing against an immutable released contextual-orchestrator API/client/schema/Agent boundary.
- **#164 release evidence** — Draft behind #77. It separates PR build/SBOM evidence from protected-main OIDC attestation authority; no feature branch may publish a final release.
- **#127 material UI/a11y** — intentionally Draft. Real browser current-head evidence remains required for keyboard skip/focus, accessible names/descriptions, live-region behavior, responsive widths and applicable normal/loading/error/permission states; source-string tests alone are not WCAG 2.2 AA evidence.
- **#90 SIEM/OpenTelemetry preservation** — stale mixed aggregate; preserve its unique Wardnet OCSF/OTLP/RFC 5424 exporter/input/fuzz/security evidence through a bounded successor from fresh protected main and do not inherit unrelated owner surfaces wholesale.

## Single-writer discipline

PR #130 remains the sole writer for this file. Other Wardnet lanes hand exact evidence into #130 instead of editing the ledger. PR retirement still requires protected merge, explicit user instruction, malicious/no-valid delta, or verified complete successor transfer of every useful code/test/fixture/contract/evidence delta.

## Context Fabric read-only inventory

Context Graph Contracts still reports `default_branch=develop`; protected `develop@99cb5468ba3c15c5e79688f53dee74724fae2d13` remains live and GitHub Releases are empty. Accepted integration/default is intended protected `main`, so `.github#1137` still owns the protect-main-first/default transition. Current root #4 is `5117383ac15cdfe3813340455cd99b92937ef47c`; source-provenance #25 is `10e5f4abc430437cfd15f79142703ac2b1fc612e`; DDD fitness #20 is `b04fa4f82bb2e8007dd5f1c660e305ffc286f1cb`; Context Assertion/CloudEvent child #21 is `cce4b4d1de359c254f271c04bbda3bf4e8ea989e`. #21 now exposes explicit `schema_version` alongside profile/admission identity and binds repository workflows to the immutable source head, but fresh exact-head workflow inventory remains absent. Mutable CGC heads remain compatibility-development evidence only; Wardnet does not mutate this stack.

EA Core likewise reports `default_branch=develop`; protected `develop@dd71e40a86385fb7861b0f1be19891a3f3e29ece` remains live and GitHub Releases are empty. DDD parent #39 is `c063570bd9177578fa75be69defd81c99e6ba2f3`; current Context Fabric/quarantine projection child #40 is `e56492fa061a7c7d7136e3bf2101fb608c183f80`, Draft on obsolete recorded #39 ancestry with zero exact-head workflows. Its connector/receipt boundary preserves `source_authority -> cloudevent_identity -> schema_version -> profile_id -> profile_version -> admission_version -> provenance`, restricts Wardnet projection to `observed`, forbids `malware_verdict` and `artifact_risk_score` as authoritative EA facts, and forbids direct foreign database/source copying. Mutable CGC/EA heads remain compatibility evidence only; Wardnet does not modify either owner repository.

## Buyer-visible gap order

Authority/security remains ahead of breadth: protected management authentication; immutable EgressWeave authorization/evidence integration; Wardnet-owned outbound reputation evaluation/evidence plus EgressWeave PEP composition; real attack-path CI and deployable public path; Agent Artifact Admission; PostgreSQL authority/tenant isolation and transactional outbox; Keyverse identity/approval and distributed admission/trusted attribution; proven Coraza/CRS and Suricata detection; immutable release evidence; telemetry/SLO/incident/restore; then final readiness against one immutable protected release identity.

The USD 20 billion ambition remains a product-quality bar, not a customer contract value, billing field or runtime pricing authority. Root `src/lib.rs` remains a modularity pressure point, not proof by itself that another deployable service boundary is justified; prefer a modular monolith until transaction/isolation/scale/deployment/reusable-owner evidence pays for a split.

## Standards and release gate

Security/runtime decisions remain traceable to current authoritative NIST/OWASP/CWE/IETF/OCI/Linux sources and primary/peer-reviewed research where implementation depends on them. Citation is design evidence, not proof that a control is shipped. Wardnet-owned production targets complete public rustdoc/docstrings plus 100% statement/branch/edge-case coverage with realistic bypass/replay/race/DoS/network/cleanup tests where applicable.

No Wardnet release is authorized at this snapshot. Release requires one exact integrated protected head with terminal current CI/security/coverage/docstrings/package/SBOM/provenance/reproducibility/review/migration/rollback/recovery/operability evidence, immutable artifact/source identity and a verified publication path. `main@a52ccd0...` has observed push validations, but governance normal-vs-bypass admission remains unresolved, GitHub Releases are empty, EgressWeave has no compatible immutable release, and multiple mandatory product/security gaps remain candidate-only. Feature-branch artifacts or attestations remain candidate evidence only.
