# Product and technical gap baseline

Snapshot date: 2026-09-07. Re-read live refs, PRs, reviews/threads, exact-head checks, rulesets, security evidence and releases before any merge, release, restack or foreign-owner handoff. This is Wardnet's sole commercial/product-technical current-state ledger, not an archive of predecessor evidence.

## Authority boundary

Wardnet owns the Rust-first gateway/SOC control plane, Agent Artifact Admission, destination-reputation evaluation, security evidence, Wardnet policy decisions and incident/accountability surfaces. `quarantine-sandbox-runtime` owns hostile execution/isolation/cleanup; EgressWeave owns reusable outbound URL/address/DNS/peer/redirect/proxy/TLS/resource authorization; `contextual-orchestrator` owns Agent/LLM/provider orchestration; `appguardrail` owns static package/security analysis. Wardnet consumes released/versioned ports or ACLs only and does not copy sibling source, query foreign application tables or treat mutable PR heads as production authority.

Protected ADR truth keeps browser acquisition, sessions, anti-bot challenges and CAPTCHA handling outside Wardnet. A Wardnet reputation `allow` is never transport authorization: an enforcement point must compose Wardnet policy with independently authoritative EgressWeave authorization, and neither side may override the other's deny.

`context-graph-contracts` remains the provider-neutral Context Fabric Shared Kernel and `enterprise-architecture-core` remains the EA Decision Plane. Wardnet reads those repositories for compatibility and owner handoff only. Wardnet verdicts, destination scores, IOCs and incidents remain Wardnet truth; EA may project architecture-relevant lifecycle/ownership/risk/remediation context through released Context Assertions but must not promote security verdicts into authoritative architecture facts.

Fresh GitHub Release inventories are empty for Wardnet, EgressWeave, contextual-orchestrator, Context Graph Contracts and EA Core. No mutable foreign-owner head is release authority.

## Protected truth and governance

Protected/default Wardnet truth is `main@a52ccd0a24a727d9349bb32def7713882d8cad1e` from #171. Organization ruleset `18156473` still requires `required_approving_review_count=1` while naming no required reviewer/team and requiring neither CODEOWNER nor last-push approval. It retains thread resolution, central required workflows, deletion/non-fast-forward protections and an `OrganizationAdmin/always` bypass. Under the declared solo-maintainer model, self-approval, bot/model-as-human approval and routine administrator bypass remain forbidden.

`.github#772` is the canonical governance owner path. Its live implementation PR `.github#1644` is currently exact `5f5d7a9e849984c052c72a08eba34f68cb694024`; PR-body receipts naming other heads are provenance, not execution authority. Source convergence is not live ruleset convergence and does not authorize Wardnet merge probes. The declared owner-plane target remains approval count 0 for the applicable solo-maintainer scope, no synthetic reviewer and no routine bypass while deterministic workflow/security/coverage/package/SBOM/provenance/thread/deletion/non-fast-forward controls remain fail closed. The current connector does not expose organization Administration-write ruleset mutation, so live reconciliation remains the separately provisioned protected owner-plane path rather than a Wardnet source mutation.

Runner/materialization remains `.github#712` authority, but the Wardnet evidence now separates two previously conflated failure classes. #176 exact `e2c8f3b3ca072668a347d64b829149559d2483d4` and #175 predecessor `2f7d504165d82f85d52a8cdaebe53662b4623826` were both published by bounded workflows using repository `GITHUB_TOKEN`; their successor `pull_request` runs terminated `action_required` before job materialization, with CI exposing `jobs=[]`. GitHub's documented `GITHUB_TOKEN` recursion/approval contract explains that state: workflow-authored PR create/update events require approval rather than automatically executing, while a GitHub App installation token or PAT can trigger normal workflow execution. The exact RCA and RED/GREEN acceptance are recorded on `.github#712` comments `5562807672` and `5562812828`.

A substantive Wardnet-local concurrency hygiene change then advanced #175 through the connected GitHub App identity to exact `96ddd7f56730e91dab8f27a34307dd3b5c0715e5`. On that same lineage, CI `34065870417`, Fuzz `34065870402`, Security `34065870401`, SAST `34065870425`, CodeQL `34065870424` and focused verifier `34065870418` materialized normally with jobs instead of becoming `action_required`; they are presently queued and therefore still non-passing. This isolates post-materialization runner capacity from the earlier token/trigger authorization defect. Wardnet will not manufacture GREEN through no-op commits, selector churn, broad reruns or predecessor evidence.

Delegated CodeQL failure ownership has advanced beyond the original matrix-template defect. `.github#1926` repaired the array-to-`env:` serialization defect, but live dispatcher authorization still rejects the App-token principal under `.github#1929`; central PR #1984 is explicitly blocked because its proposed single-principal canonicalization would discard a proven legitimate `github-actions[bot]` scheduler path. The least-widening current acceptance is machine-only admission of the intended exact principals with actor=sender equality, followed by an authenticated terminal verdict bound to the exact repository/head/language/run/job tuple. Wardnet does not rerun consumers or weaken CodeQL until that canonical owner path and live settings are actually converged.

## Outbound site reputation

### Proposed architecture #173

#173 remains the design/ADR foundation on protected-main ancestry. Its contract keeps acquisition and transport ownership outside Wardnet, preserves monitor/shadow operation separately from protect enforcement, and defines business exceptions as bounded Wardnet policy evidence rather than network authority. Exact head `7d0006b0f1fd3311c891bf359bd0c3e66a1831ec` is Ready/mechanically mergeable with CI `34025817869`, Security `34025817908`, SAST `34025817866` and CodeQL `34025817876` terminal success and no unresolved inline review thread. It is still not Accepted or release authority merely because its documentation checks passed; ordinary merge remains blocked by the live solo-governance defect rather than a product-source gap.

### Reputation core #175

#175 remains Draft on protected `main@a52ccd0...`. The v1 observable-URL limit drift has a complete test-first source repair lineage. Test-only exact `dd5a82df0d5588b7f35003fb7447902c10641719` obtained hosted causal RED in CI `34060554728` / rust job `101560199069`: the real `ubuntu-24.04` runner checked out the exact source, formatting passed, and the Test step failed because an exactly 8 KiB `ObservableUrl` still hit the generic 1 KiB `subject.value` bound. Bounded repair run `34061489186` then completed successfully and non-force advanced the branch to `2f7d504165d82f85d52a8cdaebe53662b4623826`, giving `ObservableUrl` the declared 8 KiB v1 limit while retaining 1 KiB for `ExactHost`, `ActualAddress` and other shared text.

The following `action_required`/zero-job state on `2f7d504...` is now classified as the documented repository-`GITHUB_TOKEN` PR-update approval path rather than a product-source failure. To remove a separate queue-amplification defect without using a no-op redispatch, current exact `96ddd7f56730e91dab8f27a34307dd3b5c0715e5` adds workflow-level PR-scoped `concurrency` plus `cancel-in-progress: true` to the temporary focused precedence verifier. That substantive App-authored synchronization caused ordinary CI/Fuzz/Security/SAST/CodeQL/focused-verifier jobs to materialize on the new exact source. All six runs remain queued, so exact-current admission is still non-passing and the temporary verifier must remain until it actually succeeds. If it reaches GREEN, remove the temporary workflow, then reacquire the entire cleanup-head gate set before any Ready or merge claim.

### Business authorization child #176

#176 remains Draft on its historical recorded parent base `#175@45f2aecd40983b785c1e37489596af641dedcc84` while live parent #175 has advanced to exact `96ddd7f56730e91dab8f27a34307dd3b5c0715e5`. Review established that an enforceable v1 reputation decision could omit the policy execution mode even though Proposed #173 distinguishes protect authorization from monitor/shadow `would_allow`/`would_deny` evidence. Exact source `eab552e2e91168267ef16dac71593832a03825ee` added the hostile missing-mode assertion; on successor `3d640f6ca39b0941396641c23310702af3b05180`, ordinary CI `34053798405` passed formatting and failed in Test while production still lacked the binding, preserving a realistic executed RED.

The initial repair mechanism was rejected because it ran PR workspace code under `contents: write`. The repaired mechanism at `c6b801e56fff501171ad0dfb75277f6db5f9bde1` used `contents: read` plus `persist-credentials: false` for hostile execution; the dependent write-authorized job executed no repository code and only performed bounded anchor-checked transformation/removal, diff validation, exact remote-head verification and non-force publication. It completed and self-removed at exact current `e2c8f3b3ca072668a347d64b829149559d2483d4`. The causal source now includes `DecisionEnvelopeV1.policy_mode`, rejects non-`protect` decisions with `WrongDecisionMode`, keeps the missing-mode hostile regression, adds monitor-mode rejection and updates TRACEABILITY. The temporary repair workflow is absent from the current tree.

This is not yet exact-head GREEN. On `e2c8f3...`, CI `34059649615`, Fuzz `34059649609` and inherited focused verifier `34059649664` are historical `action_required` zero-job receipts from the repository-`GITHUB_TOKEN` publication path. Do not churn or restack this child merely to change trigger identity. #176 stays Draft until #175 obtains executable exact-source evidence, cleans its temporary verifier, and reaches protected truth; then non-force adopt that exact protected parent while retaining only the child-owned authorization/policy-mode delta and reacquire all then-live gates.

EgressWeave issue #237 remains the canonical Rust-consumer owner path for immutable outbound authorization/evidence. EgressWeave still has no GitHub Release. Wardnet #136/#115 stay preservation/evidence lanes until a compatible immutable owner release exists; do not promote their local destination/DNS/redirect/proxy/TLS implementation.

## Other current security/product lanes

- **#166 exact-source CI evidence/queue repair** — Ready exact `e00d61c994d3b1c2b7923bb1b781c1e06d32aae4` on protected `main@a52ccd0...`. Without source churn, CI `34055794089`, Fuzz `34055793922`, Security Scan `34055793976`, and SAST Semgrep `34055793970` are now terminal success, proving the repository-owned exact-source checkout/concurrency repair can execute normally. CodeQL PR `34055793933` remains the only failing repository workflow: job `101552071393` acquired `ubuntu-24.04`, dispatched the exact current head successfully, then failed closed because `VERDICT_STATE=pending`; no authenticated `codeql-dispatch/actions` status has appeared yet. `.github#712` comment `5562188970` narrows the owner acceptance to canonical terminal CodeQL producer identity/verdict plus the prescribed exact-job rerun. Do not churn #166 or transfer predecessor evidence.
- **#167 MISP admission + shared DNSBL ownership** — Draft exact `0c83cd5956f512d79c6600e823fcfa6d6f32af4e` on protected main. CI/Fuzz/Security/SAST are terminal success; CodeQL fails only at the central delegated verdict boundary. Source GREEN covers affirmative MISP `to_ids`, active attribute/Object lifecycle, shared feed/operator DNSBL ownership, stale withdrawal, restart and persistence rollback/retry. Keep #168/#172 open until the complete delta reaches protected truth.
- **#170 MISP threat-level severity** — Draft exact `2c8d499a5772b0be64d7cc3fc42ed2825ba1499e` on exact #167 parent. Its effective child delta is only severity source/docs/tests and current CI/Fuzz are terminal success. It cannot integrate before #167; after parent protection it must non-force adopt/retarget and reacquire then-live gates.
- **#174 CodeQL SARIF uploader** — Draft exact `028caa05167f9e8f2589b681a8f79c633f406c30`, one path/one commit on protected main. CI/Security/SAST are terminal success; CodeQL is fail-closed at the delegated verdict boundary. Central owner repair, not consumer source churn, remains causal.
- **#129 Agent Artifact Admission** — exact `14c0af32e7e4f68c682d55a1b1117629fa940336` preserves Wardnet's structured pre-execution admission authority only. CI/Fuzz/Security/Semgrep are terminal success; CodeQL requests the current-head scan and fails only at delegated terminal-verdict enforcement. Downloaded-byte integrity, static scanning, hostile execution, activation and provider orchestration remain separate owner authorities.
- **#140 Runtime Configuration** — Ready exact `a904558b79ef392515dff4b501692112ad6beabf` and remains the single Wardnet supporting/bootstrap owner. CI/Fuzz/Security/SAST are terminal success; CodeQL is the sole failing exact-head lane after current-head dispatch and terminal-verdict enforcement. Dependent trusted-proxy/admission work must consume it rather than create competing environment/configuration authority.
- **#155 management authentication** remains the fail-closed non-loopback management prerequisite.
- **#93 deterministic persistence fault seam** remains the owner-correct replacement for permission-dependent failure testing.
- **#95 Coraza/CRS mixed aggregate** is preservation only. Reconstruct unique proven-engine WAF/IDS evidence into a bounded #86 successor rather than merge unrelated egress/database/release/auth changes.
- **#88 contextual-orchestrator/LiteLLM aggregate** is preservation only until an immutable CO API/client/schema/Agent release exists. Wardnet must retain only its own request/framing/header/streaming security evidence through a thin released ACL and remove provider/model/routing ownership.

## Context Fabric read-only inventory

Context Graph Contracts remains a foreign single-writer owner. Wardnet consumes only immutable released provider-neutral contract/provenance and does not edit its source or PR state. Context Assertion and default/protection work remain unreleased owner-plane state; no mutable foreign head is Wardnet production authority.

EA Core likewise remains a foreign single-writer Decision Plane. Wardnet technology/risk lifecycle may be projected through released Context Assertions, but Wardnet verdict/reputation truth stays in Wardnet and EgressWeave remains outbound transport authorization authority. Branch/default/protection repair for CGC/EA remains owner automation work, not a Wardnet source change or user choice.

## Buyer-visible gap order

Authority and safety remain ahead of feature breadth. The next release-blocking sequence is: satisfiable protected governance and exact-head control-plane evidence; protected management authentication; outbound reputation contract completion plus immutable EgressWeave authorization/evidence integration; deployed Strix attack-path evidence and proven Coraza/CRS + Suricata enforcement; Agent Artifact Admission; PostgreSQL production authority/RLS/tenant isolation; transactional outbox/idempotent workers; Keyverse-backed identity/approval and distributed admission/trusted attribution; immutable package/image/SBOM/provenance/reproducibility/promotion/rollback; production telemetry/SLO/incident/restore evidence; then one immutable protected Wardnet release identity.

The USD 20 billion ambition is a product-quality bar, not tenant pricing, ARR, billing truth or a reason to change customer-contract thresholds. Root `src/lib.rs` remains modularity pressure, not proof that another deployable service boundary is justified; prefer a modular monolith until transaction/isolation/scale/deployment/reuse evidence pays for a split.

## Release gate

No Wardnet release is authorized at this snapshot. Release requires one exact protected integrated head with terminal current CI/security/coverage/docstrings/package/SBOM/provenance/reproducibility/review/thread/migration/rollback/recovery/operability evidence; immutable package/image/source identity; verified publication; and no unresolved valid security/buyer gaps required for the declared release. Feature-branch artifacts, mutable foreign heads, queued workflows, predecessor GREEN and implicit/routine administrator bypass are non-passing evidence.