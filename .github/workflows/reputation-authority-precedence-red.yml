name: Reputation authority precedence verification

on:
  pull_request:
    paths:
      - '.github/workflows/reputation-authority-precedence-red.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: macos-15-arm64
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: e040201660c3a9d76deb91a81dd4c53b3ca9e5e2
          fetch-depth: 1
      - name: Assert exact causal GREEN source
        run: test "$(git rev-parse HEAD)" = "e040201660c3a9d76deb91a81dd4c53b3ca9e5e2"
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable
        with:
          toolchain: stable
      - name: Check formatting
        run: cargo fmt --check
      - name: Test reputation core
        run: cargo test --locked -p wardnet-reputation-core
      - name: Clippy reputation core
        run: cargo clippy --locked -p wardnet-reputation-core --all-targets -- -D warnings
